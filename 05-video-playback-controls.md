# 视频播放控制


## 5.1 自动播放单词

在词表中按下快捷键 1-9(假设为数字n), 将自动从当前一个或者多个视频中检索第一次出现该单词的片段, 并循环播放该片段n遍. 


## 5.2 自动播放多个区段

在词汇点播面板或者AB区段面板, 选中要播放的片段, 按下 Ctrl+数字,或者右键选择播放次数, 即可实现, 对选中所有视频片段重复n遍播放.


## 5.3 按独白快进

播放过程中, 按下 Alt + 左右方向键, 快速定位上一句对比, 下一句对比.

## 5.2 基本播放控制

| 操作 | 快捷键 | 说明 |
|---|---|---|
| 播放 / 暂停 | `空格` | 最常用操作 |
| 快进 5 秒 | `→` 方向键 | 小幅快进 |
| 快退 5 秒 | `←` 方向键 | 小幅快退 |
| 快进 30 秒 | `Shift + →` | 大幅快进 |
| 快退 30 秒 | `Shift + ←` | 大幅快退 |
| 上一条字幕 | `A` | 跳转到上一条字幕起始时间 |
| 下一条字幕 | `D` | 跳转到下一条字幕起始时间 |
| 重播当前字幕 | `R` | 从当前字幕起始时间重新播放 |
| 全屏 / 退出全屏 | `F` 或 `F11` | 切换全屏模式 |
| 调节音量 | `↑` / `↓` | 增大/减小音量 |
| 静音 | `M` | 切换静音 |


---

## 5.3 字幕跳转

通过字幕跳转可以精准定位到包含特定单词的视频片段：

1. 在任意词汇列表中找到目标单词
2. **单击**单词 → 跳转到该单词所在字幕的时间点
3. **双击**单词 → 跳转并自动播放

---

## 5.4 进度条操作

- **点击进度条任意位置**：跳转到对应时间
- **拖动进度条滑块**：连续跳转
- **鼠标悬停在进度条上**：显示当前时间点的预览

---

## 5.5 全屏模式

全屏模式下，词汇功能仍然可用：

- 字幕覆盖显示在视频上方
- 按 `F` 或 `Esc` 退出全屏
- 按 `空格` 暂停，可查看当前字幕词汇（鼠标移到屏幕边缘会显示词汇面板）

